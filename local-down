#!/bin/sh
set -eu

project_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
cd "$project_dir"
docker compose down --remove-orphans
printf 'Local LAB containers are stopped; durable volumes and local certificates were preserved.\n'
