# Mermaid Diagrams

These files are Mermaid source diagrams for the presentation. They describe the implemented local prototype. `logging-flow.mmd` intentionally shows Docker JSON logging rather than ELK because an ELK stack is not present in the repository.
